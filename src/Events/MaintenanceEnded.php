<?php

declare(strict_types=1);

namespace AllSystems\Laravel\Events;

/**
 * Stub — B3 replaces this with the real event carrying the parsed
 * `MaintenancePayload`. Exists now only so the service provider's listener
 * registration has a class to reference.
 */
final class MaintenanceEnded
{
}
